<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools">

    <ImageView
        android:id="@+id/ivImageHolder"
        android:layout_margin="8dp"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        tool:src="@drawable/ic_folder_default"/>

    <ImageView
        android:id="@+id/ivFileLockedIcon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@id/ivImageHolder"
        app:layout_constraintLeft_toLeftOf="@id/ivImageHolder"
        android:layout_marginTop="4dp"
        android:layout_marginStart="4dp"
        android:src="@drawable/ic_lock"
        android:visibility="gone"
        tool:visibility="visible"/>

    <TextView
        android:id="@+id/tvDirTitle"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        app:layout_constraintTop_toBottomOf="@id/ivImageHolder"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginTop="-15dp"
        android:textColor="@color/folderNameColor"
        android:textStyle="bold"
        android:textSize="14sp"
        android:visibility="gone"
        tool:visibility="visible"
        tool:text="Testing..."/>

</androidx.constraintlayout.widget.ConstraintLayout>