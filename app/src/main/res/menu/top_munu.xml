<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/miTakePhoto"
        android:icon="@drawable/ic_photo_add"
        app:showAsAction="always"
        tools:ignore="MenuTitle" />

    <item
        android:id="@+id/miImportPhoto"
        android:icon="@drawable/ic_photo_import"
        app:showAsAction="always"
        tools:ignore="MenuTitle" />

    <item
        android:id="@+id/miCreateFolder"
        android:icon="@drawable/ic_folder_add"
        app:showAsAction="always"
        tools:ignore="MenuTitle" />

    <item
        android:id="@+id/miChangeSpan"
        android:icon="@drawable/ic_view_module"
        app:showAsAction="always"
        tools:ignore="MenuTitle" />
</menu>